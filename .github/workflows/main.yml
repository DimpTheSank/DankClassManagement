name: Keep App Awake

on:
  schedule:
    - cron: '0 */6 * * *' # Chạy mỗi 6 tiếng một lần
  workflow_dispatch: # Cho phép bấm chạy tay nếu muốn

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://TEN-APP-CUA-BAN.streamlit.app
